{
  "$schema": "https://railway.com/railway.schema.json",
  "$comment": "Reference mirror — canonical config lives at monorepo root: /railway.json + /nixpacks.toml",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm install --frozen-lockfile && pnpm db:generate && pnpm build && pnpm verify:dist && pnpm --filter @tsc/api --prod deploy /app/deploy && node scripts/verify-deploy-bundle.mjs"
  },
  "deploy": {
    "startCommand": "node scripts/railway-start.mjs",
    "healthcheckPath": "/api/health/ready",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
