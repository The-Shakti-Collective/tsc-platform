name: runtime-validation

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

concurrency:
  group: runtime-validation-${{ github.ref }}
  cancel-in-progress: true

env:
  NODE_VERSION: "20"
  PNPM_VERSION: "9.15.0"
  DATABASE_URL: postgresql://tsc:tsc@localhost:5432/tsc_ci

jobs:
  runtime-artifacts:
    name: runtime-artifacts
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm monorepo
        uses: ./.github/actions/setup-pnpm
        with:
          node-version: ${{ env.NODE_VERSION }}
          pnpm-version: ${{ env.PNPM_VERSION }}

      - name: Generate Prisma client
        run: pnpm db:generate

      - name: Build API dependency graph
        run: pnpm build

      - name: Verify workspace dist artifacts
        run: pnpm verify:dist

      - name: Create production deploy bundle
        run: pnpm deploy:api

      - name: Verify deploy bundle artifacts
        run: pnpm verify:deploy

      - name: CoreKnot P0 migration dry-run
        env:
          MONGODB_URI: mongodb://127.0.0.1:27017/taskmaster_ci
        run: pnpm migrate:coreknot:p0 || echo "P0 dry-run skipped (no Mongo in CI — non-blocking)"

      - name: CoreKnot cutover profile validation
        env:
          COREKNOT_POSTGRES_ENABLED: "true"
          COREKNOT_POSTGRES_PRIMARY: "true"
          COREKNOT_MONGO_REQUIRED: "false"
          COREKNOT_AUTH_STORE: postgres
          COREKNOT_TENANT_STORE: postgres
          COREKNOT_PROJECTS_STORE: postgres
          COREKNOT_TASKS_STORE: postgres
          COREKNOT_CRM_STORE: postgres
          COREKNOT_ARTISTS_STORE: postgres
          COREKNOT_MAIL_STORE: postgres
          COREKNOT_DATAHUB_STORE: postgres
          COREKNOT_ATTENDANCE_STORE: postgres
          COREKNOT_FINANCE_STORE: postgres
          COREKNOT_NEWSLETTER_STORE: postgres
          COREKNOT_INTEGRATIONS_STORE: postgres
          COREKNOT_GAMIFICATION_STORE: postgres
          COREKNOT_CALENDAR_STORE: postgres
          COREKNOT_NOTIFICATIONS_STORE: postgres
          COREKNOT_DISABLE_GRIDFS_BACKUP: "true"
          BACKUP_DESTINATION: neon
          SUPABASE_SECONDARY_ENABLED: "false"
        run: pnpm migrate:coreknot:verify-cutover

      - name: Resolve @tsc/database from deploy bundle
        working-directory: deploy
        run: |
          node -e "const fs=require('fs'); const p=require.resolve('@tsc/database'); if(!fs.existsSync(p)) { console.error('missing', p); process.exit(1); } console.log('resolved', p);"
          node -e "const d=require('@tsc/database'); if(!d.slugifyWorkspace) { console.error('exports empty'); process.exit(1); } console.log('require ok');"

      - name: Smoke start API from deploy bundle (5s)
        working-directory: deploy
        env:
          PORT: "4099"
          TSC_AUTH_STUB: "true"
          REDIS_URL: redis://127.0.0.1:6379
        run: |
          timeout 8s node dist/main.js || code=$?
          if [ "${code:-0}" -eq 124 ] || [ "${code:-0}" -eq 137 ]; then
            echo "API booted (timeout expected without DB)"
            exit 0
          fi
          exit ${code:-0}
