import React from 'react';
import FeaturesPage from '../marketing/FeaturesPage';

/** Marketplace V2 — services, bookings, vendors (uses existing features scaffold). */
export default function MarketplaceHub() {
  return <FeaturesPage />;
}
