/**
 * LOCKED — Harmonic Frequency brand mark (#99). White on green.
 * Do not change without explicit user unlock. See docs/LOGO_LOCKED.md
 */

/** Spoke stroke + hub dot */
export const LOGO_MARK_STROKE = '#ffffff';
export const LOGO_MARK_FILL = '#ffffff';

/** Shell behind mark in UI (brand green) */
export const LOGO_SHELL_BG = '#126d5e';

/** Scale from hub only — size, not shape */
export const LOGO_MARK_SCALE = 1.15;

export const LOGO_HUB_R = 2.2;
export const LOGO_SPOKE_STROKE_WIDTH = 0.85;
