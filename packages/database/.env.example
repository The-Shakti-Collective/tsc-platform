# Prisma CLI (`pnpm db:push`, `pnpm db:migrate`, `pnpm db:studio`)
# Copy to packages/database/.env — or use the same DATABASE_URL as repo root `.env`.
# Never commit secrets.

DATABASE_URL=postgresql://postgres:postgres@localhost:5432/tsc_community
